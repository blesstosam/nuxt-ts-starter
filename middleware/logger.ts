import { Middleware } from '@nuxt/types';

/* eslint-disable-next-line */
const logger: Middleware = (context) => {
  // console.log('...........in logger.............', context.req.headers);
};

export default logger;
